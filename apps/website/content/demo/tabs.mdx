## Tabs

Swap between related panels without losing context.

<Tabs groupId="release">
  <Tab value="Product">
    Frame the story, set success criteria, and share rollout dates in one place.
  </Tab>
  <Tab value="Engineering">
    Ship behind a flag, monitor early adopters, then expand slowly.

    ```bash
    pnpm add @prose-ui/react @prose-ui/style
    ```
  </Tab>
  <Tab value="Design">
    Lock in the states: resting, hover, focus, error, and loading. Link every
    interaction to a token.
  </Tab>
</Tabs>
